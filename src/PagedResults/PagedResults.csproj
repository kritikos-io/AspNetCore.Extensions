<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
  </PropertyGroup>

  <PropertyGroup Label="Package">
    <Description>Generic offset-pagination result types with Entity Framework Core query helpers that materialize an IQueryable into a page plus total-count metadata.</Description>
    <PackageTags>pagination;paging;offset-pagination;entity-framework-core;efcore;queryable</PackageTags>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.EntityFrameworkCore"/>
  </ItemGroup>
</Project>
