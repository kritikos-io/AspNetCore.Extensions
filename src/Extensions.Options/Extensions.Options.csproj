<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
  </PropertyGroup>

  <PropertyGroup Label="Package">
    <Description>Static-abstract contracts that let an options class declare the configuration section it binds from, so the section location lives with the type instead of at every call site.</Description>
    <PackageTags>options;configuration;ioptions;binding;dotnet</PackageTags>
  </PropertyGroup>

</Project>
